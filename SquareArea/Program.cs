using System;

namespace ProgrammingBasics
{
    class Program
    {
        static void Main(string[] args)
        {
            int square = int.Parse(Console.ReadLine()); // 2
            int total = square * square;
            Console.WriteLine(total);
        }
    }
}
